# Employee-Lifecycle-Platform
A scalable Human Resource Management and People Operations platform built with FastAPI, featuring employee management, onboarding, authentication, and workflow automation.
Employee Lifecycle Platform

A backend Employee Management System built with FastAPI and SQL Server, featuring JWT authentication, role-based access control, and a Retrieval-Augmented Generation (RAG) assistant for HR policy Q&A powered by a locally-run LLM.

Table of Contents:
1. Overview
2. Features
3. Architecture
4. Tech Stack
5. Project Structure
6. Getting Started
7. API Reference
8. Authentication & Roles
9. HR Policy Q&A (RAG)
10. Roadmap
11. Known Limitations
12. License



1. **Overview**
This project simulates a real-world Employee Management System (in the spirit of platforms like Workday) as a learning project covering production-relevant backend concepts: REST API design, relational data modeling, authentication/authorization, and applied LLM integration via RAG.
It was built incrementally, with each feature added as a deliberate learning step — see Roadmap for what's implemented versus designed-but-not-built.

2. **Features**
Employee CRUD — create, read, update, and delete employee records
JWT Authentication — secure register/login flow using signed, time-limited tokens
Password Security — bcrypt hashing (never stores plaintext passwords)
Role-Based Access Control (RBAC) — **admin** and **employee** roles, enforced via FastAPI dependencies
HR Policy Q&A (RAG) — employees can ask natural-language questions about company policy; answers are generated by a locally-hosted LLM, grounded in actual policy documents (not the model's general knowledge)
Auto-generated API docs — full interactive Swagger UI at **/docs**


3. **Architecture**

                    Client (Swagger UI / future frontend)
                                  |
                                  v  HTTPS (JSON)
                         FastAPI Application
                                  |
              +-------------------+-------------------+
              |                   |                   |
           auth.py             crud.py               rag/
        (JWT, bcrypt,       (DB operations)      (ingest.py,
            RBAC)                                 qa_chain.py)
              |                   |                   |
              v                   v                   v
        database.py          schemas.py           ChromaDB
      (SQLAlchemy engine     (Pydantic            (local vector
        + session)            validation)            store)
              |                                        |
              v                                        v
         SQL Server                              Ollama (phi3)
         Express (SSMS)                          local LLM, served
       employeedetails, users                    via REST API

**Request flow example (GET /employees):**
  1. Request hits the route, FastAPI resolves dependencies first
  2. **get_current_user** validates the JWT — invalid/missing tokens short-circuit with **401** before any business logic runs
  3. **crud.get_all()** queries the database via SQLAlchemy
  4. The response is validated and serialized through a Pydantic **response_model**

4. **Tech Stack**

**Layer  TechnologyAPI** 
Framework          FastAPI
ORM                SQLAlchemy
Database           SQL Server Express
Validation         Pydantic v2
Authentication     python-jose (JWT), passlib (bcrypt)
RAG Orchestration  LangChain
Vector Store       ChromaDB
Embeddings         sentence-transformers (**all-MiniLM-L6-v2**)
LLM Runtime        Ollama running **phi3**
Server             Uvicorn

5. **Project Structure**

Employee-Lifecycle-Platform/
├── main.py                # FastAPI app instance & route definitions
├── database.py            # DB engine, session factory, connection config
├── models.py               # SQLAlchemy ORM table definitions
├── schemas.py              # Pydantic request/response schemas
├── crud.py                  # Database operation functions
├── auth.py                  # Password hashing, JWT creation/verification, RBAC
├── rag/
│   ├── __init__.py
│   ├── ingest.py            # Document loading, chunking, embedding, vector storage
│   └── qa_chain.py          # Retrieval + LLM-based answer generation
├── hr_policies/             # Source HR policy documents (PDF/DOCX) — not committed
├── requirements.txt
└── README.md

6. **Getting Started**

**Prerequisites**
  1. Python 3.11+
  2. SQL Server Express + SSMS
  3. ODBC Driver 17 (or 18) for SQL Server
  4. Ollama (for the RAG feature)

**Installation**

bash# Clone the repository
git clone https://github.com/chiranjeeva2002/Employee-Lifecycle-Platform.git
cd Employee-Lifecycle-Platform

**Create and activate a virtual environment**
python -m venv venv
venv\Scripts\activate        --Windows
**source venv/bin/activate**   --macOS/Linux

**Install dependencies**
pip install -r requirements.txt

**Database Setup**
  1. In SSMS, create the database:
      sql   CREATE DATABASE EmployeeDB;
  2. Update the connection string in database.py with your SQL Server instance name:
      python   DATABASE_URL = (
       "mssql+pyodbc://YOUR_SERVER\\SQLEXPRESS/EmployeeDB"
       "?driver=ODBC+Driver+17+for+SQL+Server"
       "&trusted_connection=yes"
      )
  3.Tables are created automatically on first run via
      models.Base.metadata.create_all().

**Environment Variables**
  Currently, SECRET_KEY is defined directly in auth.py. For any real deployment, move it to a .env file (see Known Limitations).
**Run the Server**
  bashuvicorn main:app --reload
  Visit http://127.0.0.1:8000/docs for the interactive Swagger UI.
**Set Up the RAG Feature (Optional)**
  bash# Install and pull a lightweight local model
  ollama pull phi3
  # Add HR policy documents (PDF or DOCX) to the hr_policies/ folder
  # Then trigger ingestion (admin only):
  POST /hr/ingest

7. **API Reference**

**Method**  **Endpoint**    **Access**  **Description**
POST         /register        Public         Register a new user (admin or employee)
POST         /login           Public         Authenticate and receive a JWT access token
GET          /me              Authenticated  Get current logged-in user's profile
GET          /employees       Authenticated  List all employees
GET          /employees/{id}  Authenticated  Get a single employee by ID
POST         /employees       Admin          onlyCreate a new employee
PUT          /employees/{id}  Admin          onlyUpdate an employee
DELETE       /employees/{id}  Admin          onlyDelete an employee
POST         /hr/ingest       Admin          onlyRe-index HR policy documents into the vector store
POST         /hr/ask          Authenticated  Ask a natural-language HR policy question

Full interactive documentation with request/response schemas is available at /docs once the server is running.

8. **Authentication & Roles**
Authentication uses **JWT (JSON Web Tokens)**:
   1. **POST /register** creates a user with a bcrypt-hashed password and an assigned role (**admin** or **employee**)
   2. **POST /login** verifies credentials and returns a signed, time-limited access token
   3. Protected routes require **Authorization: Bearer <token>;** the token is verified on every request via a FastAPI dependency
   4. Admin-only routes layer an additional role check (**require_admin**) on top of authentication


**Role   Can view employees   Can create/update/delete employees   Can re-index HR docs**
employee Yes                  No                                   No
admin    Yes                  Yes                                  Yes

9. **HR Policy Q&A (RAG)**
Rather than relying on the LLM's general training knowledge (which has no awareness of this company's actual policies), the system uses **Retrieval-Augmented Generation:**
  1. Ingestion — HR policy documents are loaded, split into overlapping text chunks, embedded into vectors, and stored in ChromaDB
  2. Retrieval — an incoming question is embedded the same way, and the most semantically similar chunks are retrieved
  3. Generation — those chunks plus the original question are passed to a locally-running phi3 model (via Ollama), with a prompt instructing it to answer only from the provided context

This grounds answers in the company's actual documents and reduces (though doesn't eliminate) hallucination, while keeping all data and inference local — no third-party API calls, no data leaving the machine.

10.  **Roadmap**
**Implemented:**
 Employee CRUD
 JWT authentication
 Role-based access control
 HR Policy Q&A via RAG

**Designed, not yet implemented:**
 Departments module (one-to-many relationship between employees and departments)
 Search, filtering & pagination on employee listing
 Leave management workflow
 Automated test suite (pytest)
 Refresh tokens
 Containerization (Docker) & CI/CD pipeline

11. **Known Limitations**
This is a learning project and intentionally documents its current gaps rather than hiding them:
  SECRET_KEY is currently hardcoded rather than loaded from an environment variable
  No automated test suite yet
  No token revocation mechanism — tokens remain valid until natural expiry
  No rate limiting on /login
  RAG ingestion is a synchronous, blocking operation
  No pagination — GET /employees returns all rows


12. **License**

This project is licensed under the MIT License — see the LICENSE file for details.
